using UnityEngine;

public class ClickSoundManager : MonoBehaviour
{

    private ClickSoundManager Instance;
    void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
